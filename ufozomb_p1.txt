<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover" />
  <title>UFOZOMB</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    html, body {
      width: 100%; height: 100%; overflow: hidden;
      background: #0a0612; font-family: "Segoe UI", system-ui, sans-serif; color: #f5e6ea;
      touch-action: none; -webkit-user-select: none; user-select: none;
    }
    #c {
      display: block; width: 100%; height: 100%;
      touch-action: none; cursor: crosshair;
    }
    #hud {
      position: fixed; inset: 0; pointer-events: none; z-index: 2;
      display: flex; flex-direction: column; justify-content: space-between;
      padding: 14px 16px; padding-bottom: max(14px, env(safe-area-inset-bottom));
    }
    .top { display: flex; justify-content: space-between; align-items: flex-start; gap: 10px; }
    .panel {
      background: rgba(26, 10, 18, 0.75);
      border: 1px solid rgba(245, 230, 234, 0.14);
      border-radius: 14px; padding: 10px 14px; backdrop-filter: blur(8px);
    }
    .score { font-size: 1.1rem; font-weight: 600; letter-spacing: -0.02em; }
    .score span { color: #c45c6a; }
    .hint { font-size: 0.78rem; opacity: 0.75; margin-top: 4px; line-height: 1.35; }

    /* Always-on finger controls */
    #controls {
      position: fixed; left: 0; right: 0; bottom: 0; z-index: 6;
      display: flex; justify-content: space-between; align-items: flex-end;
      padding: 0 18px max(22px, env(safe-area-inset-bottom));
      pointer-events: none;
    }
    .pad {
      pointer-events: auto;
      width: 148px; height: 148px; border-radius: 50%;
      background: rgba(255, 255, 255, 0.12);
      border: 2px solid rgba(255, 255, 255, 0.28);
      position: relative;
      touch-action: none;
    }
    .stick {
      position: absolute; width: 64px; height: 64px; border-radius: 50%;
      background: rgba(196, 45, 74, 0.9);
      border: 2px solid rgba(255, 180, 200, 0.5);
      left: 50%; top: 50%;
      transform: translate(-50%, -50%);
      box-shadow: 0 4px 16px rgba(0,0,0,0.35);
      pointer-events: none;
    }
    .fire {
      pointer-events: auto;
      width: 100px; height: 100px; border-radius: 50%;
      display: grid; place-items: center;
      font-weight: 700; font-size: 0.9rem; letter-spacing: 0.06em;
      color: #fff;
      background: rgba(196, 45, 74, 0.75);
      border: 2px solid rgba(255, 160, 180, 0.55);
      touch-action: none;
      box-shadow: 0 0 28px rgba(155, 27, 58, 0.45);
    }
    .fire:active, .fire.held {
      transform: scale(0.94);
      background: rgba(220, 60, 90, 0.9);
    }

    #overlay {
      position: fixed; inset: 0; display: flex; align-items: center; justify-content: center;
      background: radial-gradient(ellipse at center, #3a1020cc 0%, #0a0612ee 70%);
      z-index: 10;
    }
    #overlay.hidden { display: none; }
    .card {
      text-align: center; max-width: 420px; padding: 28px 24px; margin: 16px;
      background: #160810; border: 1px solid #5a2030; border-radius: 20px;
      box-shadow: 0 20px 60px #000a;
    }
    .card h1 {
      font-size: clamp(1.5rem, 5vw, 2.1rem); font-weight: 600;
      letter-spacing: -0.03em; margin-bottom: 8px;
    }
    .card h1 em { font-style: normal; color: #9b1b3a; }
    .card p { opacity: 0.8; line-height: 1.5; margin-bottom: 18px; font-size: 0.95rem; }
    #start {
      pointer-events: auto; cursor: pointer; border: none;
      background: linear-gradient(180deg, #c42d4a, #7a1228);
      color: #fff; font-weight: 600; font-size: 1.05rem;
      padding: 14px 32px; border-radius: 999px;
      box-shadow: 0 0 24px #9b1b3a66, inset 0 1px 0 #ff6a8899;
    }
    #start:active { transform: scale(0.98); }
  </style>
</head>
<body>
  <canvas id="c"></canvas>
  <div id="hud">
    <div class="top">
      <div class="panel">
        <div class="score">Score <span id="score">0</